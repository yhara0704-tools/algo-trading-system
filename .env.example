# Algo Trading Terminal — Environment Variables

# Coinbase Advanced Trade API (optional — public WS doesn't need auth)
# COINBASE_API_KEY=
# COINBASE_API_SECRET=

# Polymarket — no auth needed for read-only market data

# Server
HOST=0.0.0.0
PORT=8000

# Log level: DEBUG, INFO, WARNING
LOG_LEVEL=INFO
